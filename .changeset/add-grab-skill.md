---
"mattpocock-skills": minor
---

Add **`grab`**, the light, in-place sibling of `pickup`: pull down a ticket (GitHub or Linear), branch off `main` in the current checkout, make the changes, then commit, push, and open a PR — no label gate, no claiming, no worktree, no port assignment. Ships promoted in `engineering/` with a docs page and an `ask-matt` route contrasting it with the worktree loop.
