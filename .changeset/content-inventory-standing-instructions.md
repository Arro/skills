---
"mattpocock-skills": patch
---

`content-inventory`: check the repo root for an optional `.content-inventory.md` of standing instructions — a folder to write each finished inventory into (as `<scope-slug>.md`), shared chrome to always exclude, extra groups to always capture. It steers scope and delivery only; an instruction to keep layout or styling is set aside and flagged.
