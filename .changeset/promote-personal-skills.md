---
"mattpocock-skills": minor
---

Promote the former `personal/` bucket into the standard buckets: the worktree ticket loop (`queue`, `pickup`, `test-drive`, `wrapup`) into `engineering/`, and `rename-thread`, `gpt-image-2`, `obsidian-vault`, `edit-article` into `productivity/`. All eight now ship in the plugin, carry docs pages, and are routed by `ask-matt`; the `personal/` bucket is gone.
